# Terraform bootstrap

Creates the versioned, encrypted S3 bucket used as remote state backend (with native S3 locking).
Applied once with local state, before any other stack.

_Planned in step 1.4._
