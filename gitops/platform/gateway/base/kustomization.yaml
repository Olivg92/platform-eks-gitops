apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared entry point. Each environment overlays what differs: TLS issuer,
# hostname, and later the cloud load balancer annotations.
resources:
  - gatewayclass.yaml
  - gateway.yaml
