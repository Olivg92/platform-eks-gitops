# Platform components

One directory per ArgoCD Application, ordered with sync waves (CRDs → operators → apps):
Gateway API controller, cert-manager, External Secrets Operator, kube-prometheus-stack, Sloth.

_Planned in step 1.2._
