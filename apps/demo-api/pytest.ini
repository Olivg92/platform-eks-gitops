[pytest]
# Make `app` importable however pytest is invoked. Without this, `pytest` fails
# at collection while `python -m pytest` works, because only the latter puts the
# current directory on the import path.
pythonpath = .
testpaths = tests
